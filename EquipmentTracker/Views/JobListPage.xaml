<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:EquipmentTracker.ViewModels"
             xmlns:models="clr-namespace:EquipmentTracker.Models"
             x:Class="EquipmentTracker.Views.JobListPage"
             x:DataType="vm:JobListViewModel"
             Title="{Binding Title}">

    <Grid RowDefinitions="Auto, *">

        <Button Grid.Row="0" 
                Text="Yeni İş Ekle (+)" 
                Command="{Binding GoToAddNewJobCommand}"
                Margin="10"/>

        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Jobs}"
                        SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:JobModel">

                    <Frame BorderColor="{StaticResource Gray600}" CornerRadius="5" Padding="10" Margin="10,5" HasShadow="True">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer 
                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:JobListViewModel}}, Path=GoToDetailsCommand}"
                                CommandParameter="{Binding .}"/>
                        </Frame.GestureRecognizers>

                        <Grid ColumnDefinitions="*, Auto">
                            <VerticalStackLayout Grid.Column="0">
                                <Label Text="{Binding JobName}" FontSize="Medium" FontAttributes="Bold"/>
                                <Label Text="{Binding JobOwner}" FontSize="Small" TextColor="{StaticResource Gray400}"/>
                            </VerticalStackLayout>
                            <Label Grid.Column="1" Text="{Binding JobNumber, StringFormat='İş No: {0}'}" VerticalOptions="Center"/>
                        </Grid>
                    </Frame>

                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>