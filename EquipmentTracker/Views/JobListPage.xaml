<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:EquipmentTracker.ViewModels"
             xmlns:models="clr-namespace:EquipmentTracker.Models"
             x:Class="EquipmentTracker.Views.JobListPage"
             x:DataType="vm:JobListViewModel"
             Title="{Binding Title}"
             BackgroundColor="White">

    <CollectionView ItemsSource="{Binding Jobs}"
                    SelectionMode="None"
                    VerticalScrollBarVisibility="Always">
        <CollectionView.Header>
            <VerticalStackLayout HorizontalOptions="Center" Padding="0" Spacing="0">

                <Frame BorderColor="Black"
                       CornerRadius="5"
                       Padding="10"
                       Margin="0,10,0,10" HasShadow="True"
                       BackgroundColor="Transparent"
                       WidthRequest="500">

                    <VerticalStackLayout Spacing="10">
                        <SearchBar Placeholder="İş Adı veya İş No ara..."
                                   PlaceholderColor="{StaticResource Gray400}"
                                   Text="{Binding SearchText}"
                                   TextColor="Black"
                                   BackgroundColor="Transparent"/>
                    </VerticalStackLayout>
                </Frame>
            </VerticalStackLayout>
        </CollectionView.Header>

        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:JobModel" >
                <Frame BorderColor="Black"
                       CornerRadius="5"
                       Padding="0"
                       Margin="0,5,0,5"
                       HasShadow="True"
                       BackgroundColor="Transparent"
                       WidthRequest="500"
                       HorizontalOptions="Center">

                    <Grid ColumnDefinitions="*, Auto" >
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer 
                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:JobListViewModel}}, Path=GoToDetailsCommand}"
                                CommandParameter="{Binding .}"/>
                        </Grid.GestureRecognizers>

                        <VerticalStackLayout Grid.Column="0" Padding="10" BackgroundColor="White">
                            <Label Text="{Binding JobName}" FontSize="Medium" FontAttributes="Bold" TextColor="Black"/>
                            <Label Text="{Binding JobOwner}" FontSize="Small" TextColor="{StaticResource Gray600}"/>
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Column="1" Padding="10" BackgroundColor="White">
                            <Label Text="{Binding JobNumber, StringFormat='İş No: {0}'}"
                                   VerticalOptions="Center"
                                   TextColor="Black"
                                   HorizontalOptions="End"
                                   Margin="0,17,70,0"/>
                        </VerticalStackLayout>

                        
                        <Button Grid.Column="1"
                                HeightRequest="20"
                                WidthRequest="68"
                                FontSize="Caption"
                                FontAttributes="Bold"
                                VerticalOptions="Center"
                                HorizontalOptions="End"
                                Margin="0,47,70,0"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:JobListViewModel}}, Path=ToggleJobStatusCommand}"
                                CommandParameter="{Binding .}"
                                
                                >
                        

                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding IsCancelled}" Value="True">
                                    <Setter Property="Text" Value="Aktif Et" />
                                    <Setter Property="BackgroundColor" Value="#4CAF50" />
                                </DataTrigger>

                                <DataTrigger TargetType="Button" Binding="{Binding IsCancelled}" Value="False">
                                    <Setter Property="Text" Value="İptal Et" />
                                    <Setter Property="BackgroundColor" Value="#D32F2F" />
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>

                        <Button Grid.Column="2"
                                HeightRequest="20"
                                WidthRequest="60"
                                FontSize="Caption"
                                FontAttributes="Bold"
                                VerticalOptions="Center"
                                HorizontalOptions="End"
                                Margin="0,45,5,0"
                                Text="Sil"
                                BackgroundColor="Red"
                                TextColor="White"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:JobListViewModel}}, Path=DeleteJobCommand}"
                                CommandParameter="{Binding .}" />

                    </Grid>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>

    </CollectionView>

</ContentPage>